import 'package:flutter/material.dart';

class FixesScreen extends StatelessWidget {
  const FixesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Fixo"),
    );
  }
}
